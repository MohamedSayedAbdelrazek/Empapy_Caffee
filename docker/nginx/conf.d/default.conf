server {
    listen 80;
    server_name empapy.com www.empapy.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name empapy.com www.empapy.com;

    # 🔒 مسارات شهادة الحماية
    ssl_certificate /etc/letsencrypt/live/empapy.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/empapy.com/privkey.pem;

    root /var/www/html/public;
    index index.php index.html;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # ========================================
    # 🚀 Gzip Compression (PageSpeed Optimization)
    # ========================================
    gzip on;
    gzip_vary on;
    gzip_min_length 10240;
    gzip_proxied expired no-cache no-store private auth;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml application/javascript application/json image/svg+xml;
    gzip_disable "MSIE [1-6]\.";

    # Fix storage path resolution using direct Alias
    location ^~ /storage/ {
        # Direct mapping to the mounted volume path inside the container
        alias /var/www/html/storage/app/public/;
        # REMOVED try_files to prevent path calculation errors with alias

        # Maintain caching for performance
        access_log off;
        expires 1y;
        add_header Cache-Control "public, no-transform";
    }

    # التعامل مع باقي الروابط
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # التعامل مع ملفات PHP
    location ~ \.php$ {
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    # ========================================
    # 📦 Browser Caching for Static Assets (1 Year)
    # ========================================
    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot|webp|mp3|wav|ogg)$ {
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "public, no-transform";
        add_header X-Content-Type-Options "nosniff" always;
        access_log off;
    }
}